# HTTP Server in C with Zig Build System
### By Alex Andrushevich

HTTP 0.9 compliant server witten from scratch in C
Using Zig's build system

To compile , use:

```console
foo@bar:~$ $ zig build

```

